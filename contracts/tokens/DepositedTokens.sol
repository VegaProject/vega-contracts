pragma solidity ^0.4.9;

import "./VegaToken.sol";

contract DepositedTokens {
  /*address public vga;
  mapping (address => uint) balances;

  function DepositedTokens(address _vga) {
    vga = VegaToken(_vga);
  }

  /// @notice This will deposit tokens from a vga holder to this contract.
  /// @param _from The address holding the tokens being transferred
  /// @param _amount The amount of tokens to be transferred
  /// @return True if the transfer was successful
  function depositTokens(address _from, uint _amount) {
    vga.transferFrom(_from, this, _amount);
    balances[_from] += _amount;
  }


  /// @notice This will withdrawal tokens from this contract to an address.
  /// @param _to The address the tokens are transferred to
  /// @param _amount The amount of tokens to be transferred
  /// @return True if the transfer was successful
  function withdrawalTokens(address _to, uint _amount) {
  }*/
}
